import { Employe } from "../model/empolye.model"

 export const logout= async (req,res)=>{
 try {
     const {email}= req.query.email
     const user= await Employe

    
 } catch (error) {
    
 }
}
